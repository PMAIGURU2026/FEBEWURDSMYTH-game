/* TEMPLATE — copy to env-config.js and fill in your real values.
   env-config.js is gitignored so secrets never reach GitHub.
   On Vercel: set SUPABASE_URL and SUPABASE_ANON_KEY as env vars instead. */
const ENV_CONFIG = {
    SUPABASE_URL: "https://YOUR_PROJECT_REF.supabase.co",
    SUPABASE_ANON_KEY: "your-anon-key-here"
};
